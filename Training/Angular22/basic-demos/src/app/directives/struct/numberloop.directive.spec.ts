import { NumberloopDirective } from './numberloop.directive';

describe('NumberloopDirective', () => {
  it('should create an instance', () => {
    const directive = new NumberloopDirective();
    expect(directive).toBeTruthy();
  });
});
